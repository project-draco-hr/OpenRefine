{
  try {
    for (    Object key : bindings.keySet()) {
      String k=(String)key;
      Object v=bindings.get(k);
      _engine.set(k,(v instanceof HasFields) ? new JythonHasFieldsWrapper((HasFields)v,bindings) : v);
    }
    Object result=_engine.eval(s_functionName + "()");
    return unwrap(result);
  }
 catch (  PyException e) {
    return new EvalError(e.toString());
  }
}
