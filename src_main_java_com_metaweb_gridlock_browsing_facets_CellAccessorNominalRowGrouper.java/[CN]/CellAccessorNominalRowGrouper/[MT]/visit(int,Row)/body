{
  if (_cellIndex < row.cells.size()) {
    Cell cell=row.cells.get(_cellIndex);
    if (cell != null) {
      Object[] values=_accessor.get(cell,true);
      if (values != null && values.length > 0) {
        for (        Object value : values) {
          if (value != null) {
            DecoratedValue dValue=value instanceof DecoratedValue ? (DecoratedValue)value : new DecoratedValue(value,value.toString());
            Object v=dValue.value;
            if (choices.containsKey(v)) {
              choices.get(v).count++;
            }
 else {
              NominalFacetChoice choice=new NominalFacetChoice(dValue);
              choice.count=1;
              choices.put(v,choice);
            }
          }
        }
      }
    }
  }
  return false;
}
