{
  writer.object();
  writer.key("type");
  writer.value(ImportManager.getImportSourceClassName(this.getClass()));
  writer.key("accessTime");
  writer.value(ParsingUtilities.dateToString(accessTime));
  writer.key("size");
  writer.value(size);
  writer.key("isArchive");
  writer.value(isArchive);
  writer.key("contentType");
  writer.value(contentType);
  writer.key("encoding");
  writer.value(encoding);
  writer.endObject();
}
