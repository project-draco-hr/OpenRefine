{
  if (obj == null) {
    obj=new JSONObject();
    try {
      obj.put(ID,node.topic.id);
      obj.put(CONNECT,"insert");
    }
 catch (    JSONException e) {
      e.printStackTrace();
    }
  }
  return obj;
}
