{
  if (namespace == null) {
    return false;
  }
  namespace=namespace.trim();
  for (  Vocabulary v : vocabularies) {
    if (v.getUri().equals(namespace)) {
      return true;
    }
  }
  return false;
}
