{
  Format record=ImportingManager.formatToRecord.get(format);
  if (record == null || record.parser == null) {
    return;
  }
  job.prepareNewProject();
  record.parser.parse(job.project,job.metadata,job,getSelectedFileRecords(job),format,100,optionObj,exceptions);
  job.project.update();
}
