{
  if (process.isImmediate() && _processes.size() == 0) {
    try {
      process.performImmediate();
    }
 catch (    Exception e) {
      e.printStackTrace();
    }
    return true;
  }
 else {
    _processes.add(process);
    update();
    return false;
  }
}
