{
  PushbackInputStream pis=new PushbackInputStream(inputStream);
  int b;
  while ((b=pis.read()) >= 0) {
    if (!Character.isWhitespace(b)) {
      pis.unread(b);
      break;
    }
  }
  return pis;
}
