{
  writer.object();
  writer.key("description");
  writer.value("Compares two Strings, and returns the portion where they differ. (More precisely, return the remainder of the second String, starting from where it's different from the first.)");
  writer.key("params");
  writer.value("string s, string v");
  writer.key("returns");
  writer.value("string");
  writer.endObject();
}
