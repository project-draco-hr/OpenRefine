{
  double largerValue=a;
  double smallerValue=b;
  if (b > a) {
    largerValue=b;
    smallerValue=a;
  }
  for (int i=1; i <= largerValue; i++) {
    if ((largerValue * i) % smallerValue == 0)     return largerValue * i;
  }
  return largerValue * smallerValue;
}
