{
  if (process.isImmediate() && _processes.size() == 0) {
    try {
      return process.performImmediate();
    }
 catch (    Exception e) {
      e.printStackTrace();
    }
  }
 else {
    _processes.add(process);
    update();
  }
  return null;
}
